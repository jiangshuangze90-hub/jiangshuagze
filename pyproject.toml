[project]
name = "jiangshuagze"
version = "0.1.0"
description = "A tiny Python command-line greeting app."
readme = "README.md"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
